info:
  name: lembrete 2
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:4000/lembretes
  body:
    type: json
    data: '{"texto":"comprar leite"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​