info:
  name: observacao 1
  type: http

http:
  method: GET
  url: http://localhost:5001/lembretes/1/observacoes
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
